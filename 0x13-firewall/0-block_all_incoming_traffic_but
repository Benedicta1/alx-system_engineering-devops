#!usr/bin/env bash
#install the ufw firewall and setup

apt install ufw
ufw disable
ufw --force reset
ufw default deny incoming
ufw default allow outgoing
systemctl stop ufw
ufw allow ssh
ufw allow http
ufw allow https
systemctl start ufw
ufw --force enable
